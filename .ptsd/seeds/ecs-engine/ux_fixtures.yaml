# ECS Engine — UX Integration Fixtures
# S10: Dashboard reads live ECS state

fixtures:
  - id: dashboard_live
    description: "Production chain running, dashboard reads EnergyPool + TierState + opus"
    grid: [10, 10]
    terrain_tiles:
      - { pos: [2, 3], type: iron_vein }
    placements:
      - { building: wind_turbine, pos: [1, 3] }
      - { building: iron_miner, pos: [2, 3] }
      - { building: iron_smelter, pos: [3, 3] }
    dashboard:
      is_open: true
    opus_nodes:
      - { index: 0, resource: iron_bar, required_rate: 0.5, tier: 1 }
    ticks: 5
